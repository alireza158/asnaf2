@extends('admin.layouts.app')
@section('title','مدیریت دسته‌بندی‌ها')
@section('content')
<div class="admin-page-toolbar"><div><p class="admin-eyebrow">دسته‌بندی‌ها</p><h2>مدیریت دسته‌بندی‌های داینامیک</h2></div><a class="admin-primary-btn" href="{{ route('admin.categories.create', ['type' => $type]) }}">ایجاد دسته‌بندی</a></div>
<div class="admin-panel-card mb-3"><form class="admin-search-form" method="GET"><select class="form-control" name="type"><option value="">همه انواع</option>@foreach($types as $value=>$label)<option value="{{ $value }}" @selected($type===$value)>{{ $label }}</option>@endforeach</select><button class="admin-primary-btn">فیلتر</button></form></div>
<div class="admin-panel-card"><div class="table-responsive"><table class="admin-table"><thead><tr><th>عنوان</th><th>نوع</th><th>ترتیب</th><th>وضعیت</th><th>عملیات</th></tr></thead><tbody>@forelse($categories as $category)<tr><td><strong>{{ $category->title }}</strong><br><small dir="ltr">{{ $category->slug }}</small></td><td>{{ $types[$category->type] ?? $category->type }}</td><td>{{ $category->sort_order }}</td><td>{{ $category->is_active ? 'فعال' : 'غیرفعال' }}</td><td><div class="admin-actions"><a href="{{ route('admin.categories.edit', $category) }}">ویرایش</a><form method="POST" action="{{ route('admin.categories.destroy', $category) }}">@csrf @method('DELETE')<button>حذف</button></form></div></td></tr>@empty<tr><td colspan="5" class="text-center text-muted">دسته‌بندی‌ای ثبت نشده است.</td></tr>@endforelse</tbody></table></div>@include('admin.partials.pagination',['paginator'=>$categories])</div>
@endsection
