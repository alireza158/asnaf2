@extends('admin.layouts.app')
@section('title', 'ویرایش کمیسیون')
@section('content')
<div class="admin-page-toolbar"><div><p class="admin-eyebrow">کمیسیون‌ها</p><h2>ویرایش {{ $commission->title }}</h2></div><a class="admin-secondary-btn" href="{{ route('admin.commissions.show', $commission) }}">بازگشت</a></div>
<form class="admin-panel-card admin-form" method="POST" action="{{ route('admin.commissions.update', $commission) }}" enctype="multipart/form-data">@csrf @method('PUT')
<div class="row g-3"><div class="col-md-6"><label class="form-label">عنوان</label><input class="form-control" name="title" value="{{ old('title',$commission->title) }}" required></div><div class="col-md-6"><label class="form-label">نامک</label><input class="form-control" name="slug" value="{{ old('slug',$commission->slug) }}" dir="ltr"></div><div class="col-md-4"><label class="form-label">وضعیت</label><select class="form-control" name="status">@foreach($statusLabels as $value=>$label)<option value="{{ $value }}" @selected(old('status',$commission->status)===$value)>{{ $label }}</option>@endforeach</select></div><div class="col-md-4"><label class="form-label">تاریخ انتشار</label><input class="form-control js-jalali-datetime" type="text" dir="ltr" name="published_at" value="{{ old('published_at',jalali_form_datetime($commission->published_at)) }}"></div><div class="col-md-4"><label class="form-label">دلیل رد</label><input class="form-control" name="rejected_reason" value="{{ old('rejected_reason',$commission->rejected_reason) }}"></div><div class="col-md-4"><label class="form-label">ترتیب</label><input class="form-control" type="number" name="sort_order" value="{{ old('sort_order',$commission->sort_order) }}" min="0"></div><div class="col-md-4"><label class="form-label">فعال</label><select class="form-control" name="is_active"><option value="1" @selected((string)old('is_active',(int)$commission->is_active)==='1')>فعال</option><option value="0" @selected((string)old('is_active',(int)$commission->is_active)==='0')>غیرفعال</option></select></div><div class="col-md-6"><label class="form-label">تصویر جدید</label><input class="form-control" type="file" name="image" accept="image/*">@if($commission->image)<small><a href="{{ Storage::url($commission->image) }}" target="_blank">مشاهده تصویر فعلی</a></small>@endif</div><div class="col-md-6"><label class="form-label">افزودن پیوست</label><input class="form-control" type="file" name="attachments[]" multiple></div><div class="col-12"><label class="form-label">اعضا (هر عضو در یک خط)</label><textarea class="form-control" name="members" rows="4">{{ old('members', collect($commission->members ?? [])->pluck('name')->implode("\n")) }}</textarea></div>@if(!empty($commission->attachments))<div class="col-12"><h4>پیوست‌های فعلی</h4>@foreach($commission->attachments as $index=>$file)<label class="d-flex gap-2"><input type="checkbox" name="existing_attachments[{{ $index }}][delete]" value="1"> حذف {{ $file['name'] ?? 'فایل' }}</label>@endforeach</div>@endif<div class="col-12"><label class="form-label">توضیحات</label><textarea class="form-control" name="description" rows="6">{{ old('description',$commission->description) }}</textarea></div></div>
<div class="mt-3 d-flex gap-2"><button class="admin-primary-btn">ذخیره</button><a class="admin-secondary-btn" href="{{ route('admin.commissions.show', $commission) }}">انصراف</a></div></form>
@endsection
